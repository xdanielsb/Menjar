<%= form_for(consumer) do |f| %>
  <% if consumer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consumer.errors.count, "error") %> prohibited this consumer from being saved:</h2>

      <ul>
      <% consumer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cedula %>
    <%= f.text_field :cedula %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
