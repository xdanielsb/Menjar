<%= form_for(offer) do |f| %>
  <% if offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :product_id_id %>
    <%= f.text_field :product_id_id %>
  </div>

  <div class="field">
    <%= f.label :provider_id_id %>
    <%= f.text_field :provider_id_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
